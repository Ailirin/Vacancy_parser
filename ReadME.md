# Парсер вакансий

REST API для поиска и получения информации о вакансиях из нескольких источников (РФ и РБ). Проект позволяет получать структурированные данные о вакансиях через единый API-интерфейс с поддержкой пагинации и фильтрации.

## Какие задачи решает / зачем нужен

Проект решает задачу централизованного доступа к информации о вакансиях с HeadHunter через единый API-интерфейс. Это полезно для:
- Агрегации вакансий из разных источников
- Создания собственных сервисов поиска работы
- Анализа рынка труда
- Интеграции поиска вакансий в другие приложения

## Технологии

- **Python 3.x** — основной язык разработки
- **Django 4.2+** — веб-фреймворк для создания REST API
- **requests** — библиотека для работы с HTTP-запросами к внешним API
- **SQLite** — база данных (для хранения настроек и будущего кэширования)

## Основные возможности

- 🔍 Поиск вакансий по ключевым словам из нескольких источников:
  - **HH.ru** — Россия
  - **HH.by** — Беларусь
  - **SuperJob.ru** — Россия (требуется SUPERJOB_API_KEY)
  - **rabota.by** — Беларусь
  - **all** — агрегация из всех доступных источников
- 📄 Пагинация результатов (настраиваемое количество вакансий на странице)
- 📊 Структурированный ответ с полной информацией о вакансиях:
  - Название вакансии
  - Название компании
  - Краткое описание
  - Диапазон зарплат (от/до) с указанием валюты
  - Режим работы (удалённо/в офисе)
  - Локация (город)
  - Прямая ссылка на вакансию на HH.ru
  - Уникальный ID вакансии
- ⚡ Обработка ошибок и таймаутов при работе с внешним API с правильными HTTP статусами
- ✅ Валидация входных параметров (page, per_page, search_phrase)
- 🧪 Покрытие тестами (13 тестов)
- 🔧 Гибкая настройка через переменные окружения

## Как запустить

1. **Клонируйте репозиторий:**
   ```bash
   git clone <url_репозитория>
   cd Vacancy_parser
   ```

2. **Создайте виртуальное окружение:**
   ```bash
   python -m venv venv
   ```

3. **Активируйте виртуальное окружение:**
   - Windows:
     ```bash
     venv\Scripts\activate
     ```
   - Linux/Mac:
     ```bash
     source venv/bin/activate
     ```

4. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```
   
   Или вручную:
   ```bash
   pip install django requests
   ```

5. **Настройте переменные окружения:**
   
   Создайте файл `.env` в директории `myparser/` со следующим содержимым:
   ```
   SECRET_KEY=ваш_secret_key_для_django
   DEBUG=True
   ALLOWED_HOSTS=localhost,127.0.0.1
   HH_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36
   SUPERJOB_API_KEY=ваш_ключ_superjob  # для SuperJob.ru — зарегистрируйтесь на https://api.superjob.ru/register/
   ```

6. **Примените миграции:**
   ```bash
   cd myparser
   python manage.py migrate
   ```

7. **Запустите сервер:**
   ```bash
   python manage.py runserver
   ```

   Сервер будет доступен по адресу: `http://127.0.0.1:8000`

## Запуск тестов

Для запуска тестов используйте команду:

```bash
cd myparser
python manage.py test parserapp.tests
```

Или с более подробным выводом:

```bash
python manage.py test parserapp.tests --verbosity=2
```

## Структура проекта

```
Vacancy_parser/
├── myparser/                    # Основная директория Django проекта
│   ├── manage.py
│   ├── db.sqlite3
│   ├── myparser/                # Настройки проекта
│   │   ├── settings.py
│   │   ├── urls.py
│   │   └── ...
│   └── parserapp/               # Основное приложение
│       ├── views.py             # API эндпоинты
│       ├── services/            # Бизнес-логика
│       │   └── hh_parser.py     # Сервис для работы с HH API
│       ├── serializers.py       # Преобразование данных
│       ├── models.py            # Модели данных
│       ├── tests.py             # Тесты
│       ├── urls.py              # Маршруты приложения
│       └── management/          # Django management команды
│           └── commands/
│               └── search_vacancies.py  # CLI для поиска вакансий
├── requirements.txt             # Зависимости проекта
└── ReadME.md                    # Документация
```

## CLI команда для поиска вакансий

Проект включает интерактивную команду для поиска вакансий через командную строку:

```bash
cd myparser
python manage.py search_vacancies --query "python developer" --source hh
```

Или в интерактивном режиме:

```bash
python manage.py search_vacancies
```

Команда поддерживает:
- Поиск вакансий
- Фильтрацию по режиму работы, городу, зарплате, валюте
- Сортировку результатов
- Сохранение результатов в файл

## Примеры использования

### Поиск вакансий через query-параметры

**Запрос:**
```bash
GET http://127.0.0.1:8000/api/search/?search_phrase=python&source=hh&page=0&per_page=5
```

**Ответ:**
```json
{
  "vacancies": [
    {
      "title": "Python Developer",
      "company_name": "ООО Технологии",
      "description": "Разработка backend-приложений на Python...",
      "salary_from": 150000,
      "salary_to": 250000,
      "currency": "RUB",
      "work_mode": "remote",
      "location": "Москва",
      "url": "https://hh.ru/vacancy/12345678",
      "external_id": "12345678",
      "source": "HH.ru"
    }
  ]
}
```

### Параметры запроса

- `search_phrase` (обязательный) — поисковый запрос
- `source` (опциональный, по умолчанию hh) — источник: hh, hh_by, superjob, rabota, all
- `page` (опциональный, по умолчанию 0) — номер страницы (должен быть неотрицательным целым числом)
- `per_page` (опциональный, по умолчанию 20) — количество вакансий на странице (от 1 до 100)

### Обработка ошибок

API возвращает соответствующие HTTP статусы:

- **200 OK** — успешный запрос
- **400 Bad Request** — ошибка валидации параметров (неверный формат page/per_page, отсутствует search_phrase)
- **502 Bad Gateway** — ошибка при запросе к HeadHunter API
- **504 Gateway Timeout** — таймаут при запросе к HeadHunter API
- **500 Internal Server Error** — внутренняя ошибка сервера

**Пример ответа с ошибкой:**
```json
{
  "error": "page must be a valid integer",
  "details": "..."
}
```

## Что было сделано лично тобой / чему научился

### Реализованный функционал:

1. **Архитектура проекта:**
   - Разделение кода на слои (views, services, serializers)
   - Создание отдельного сервиса `HHParser` для работы с внешним API
   - Использование serializers для преобразования данных из формата HH.ru в нужный формат

2. **REST API эндпоинт:**
   - Реализация class-based view `VacancySearchView`
   - Поддержка получения параметров через query-параметры
   - Валидация всех входных параметров с понятными сообщениями об ошибках
   - Правильные HTTP статусы для различных типов ошибок

3. **Интеграция с внешним API:**
   - Работа с официальным API HeadHunter (https://api.hh.ru/vacancies)
   - Настройка заголовков запросов (User-Agent, Accept)
   - Обработка таймаутов и ошибок сети с кастомными исключениями
   - Парсинг и преобразование ответа API в структурированный формат
   - Правильная обработка ошибок с пробросом HTTP статусов от HH API

4. **Обработка данных:**
   - Извлечение и форматирование информации о зарплате (с учётом валюты)
   - Определение режима работы (удалённо/в офисе) на основе данных API
   - Обработка отсутствующих полей в ответе API

5. **Настройка проекта:**
   - Конфигурация Django для работы с переменными окружения
   - Настройка middleware для отключения CSRF для API-эндпоинтов
   - Структурирование проекта по best practices Django
   - Создание requirements.txt для управления зависимостями

6. **Тестирование:**
   - Написание unit-тестов для API эндпоинта (валидация параметров, обработка ошибок)
   - Тесты для сериализаторов (обработка полных и неполных данных)
   - Тесты для определения режима работы
   - Всего 13 тестов, покрывающих основные сценарии использования

### Чему научился:

- ✅ Работа с Django REST API и создание собственных эндпоинтов
- ✅ Интеграция с внешними API (HeadHunter)
- ✅ Обработка ошибок и исключений при работе с внешними сервисами с правильными HTTP статусами
- ✅ Валидация входных данных в API с понятными сообщениями об ошибках
- ✅ Структурирование кода: разделение на слои (views, services, serializers)
- ✅ Работа с переменными окружения и настройка конфигурации
- ✅ Парсинг и преобразование JSON-данных
- ✅ Использование библиотеки `requests` для HTTP-запросов
- ✅ Написание unit-тестов для Django приложений
- ✅ Создание кастомных исключений для обработки ошибок
- ✅ Понимание архитектуры REST API и принципов работы с ними

